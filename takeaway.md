# TAKEAWAY.md — 디버깅 로그
# Cursor가 에러/버그 해결 시 자동으로 기록하는 파일
# 프로젝트 init 시 루트에 배치할 것

---

## 기록 규칙

이 파일은 Cursor(AI)가 작성한다. 사람이 직접 쓰지 않아도 됨.
트리거 조건과 포맷은 CLAUDE.md의 `## Takeaway 자동 기록 규칙` 섹션 참고.

---

## 로그

<!-- 
Cursor가 아래 포맷으로 맨 아래에 추가한다.
가장 최근 기록이 파일 하단에 온다 (시간순).

### [YYYY-MM-DD] 한 줄 제목
- **증상**: 사용자가 본 문제 (뭐가 안 됐는지)
- **원인**: 왜 그랬는지 (근본 원인)
- **해결**: 뭘 바꿨는지 (코드 변경 요약)
- **교훈**: 다음에 같은 실수 안 하려면 (한 줄)
- **태그**: `#카테고리` (아래 태그 목록에서 선택)

태그 목록:
#fetch-defense  — 외부 URL/이미지 fetch 관련
#api-error      — OpenAI API 또는 외부 API 호출 실패
#deploy         — 배포 환경에서만 발생하는 문제 (Railway, Vercel 등)
#db             — MongoDB 연결/쿼리 문제
#cors           — CORS 관련
#env            — 환경변수 누락/잘못 설정
#state          — React 상태 관리 버그
#async          — 비동기 처리 순서/타이밍 문제
#ui             — UI 렌더링/스타일 깨짐
#auth           — 인증/권한 관련
#other          — 위에 해당 안 되는 것
-->
