# Client (Vite + React)

Project overview, local run steps, env vars, and Atlas setup live in the **[root README.md](../README.md)**.

GitHub shows the repository homepage from the **root `README.md` only** by default.
